[project]
name = "oss-f01-fixture"
version = "0.1.0"
description = "Disposable fixture for oss-agent F0.1 rehearsal (not a real package)."
requires-python = ">=3.11"
dependencies = ["pytest"]

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
